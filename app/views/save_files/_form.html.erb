
<%= simple_form_for(@save_file) do |f| %>
  <%= f.error_notification %>
  <%= f.error_notification message: f.object.errors[:base].to_sentence if f.object.errors[:base].present? %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.association :game %>
    <%= f.input :notes %>
    <%= f.input :sram %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
